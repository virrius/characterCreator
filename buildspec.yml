
version: 0.2
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file Template.yaml  --s3-bucket charactercreator 
                                   --output-template-file outputTemplate.yaml
artifacts:
  type: zip
  files:
    - Template.yaml
    - outputTemplate.yaml
